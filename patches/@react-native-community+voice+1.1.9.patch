diff --git a/node_modules/@react-native-community/voice/android/build.gradle b/node_modules/@react-native-community/voice/android/build.gradle
index 091c5ae..86bab4d 100644
--- a/node_modules/@react-native-community/voice/android/build.gradle
+++ b/node_modules/@react-native-community/voice/android/build.gradle
@@ -2,7 +2,8 @@ apply plugin: 'com.android.library'
 
 repositories {
     mavenLocal()
-    jcenter()
+    mavenCentral()
+    google()
     maven {
         // For developing the library outside the context of the example app, expect `react-native`
         // to be installed at `./node_modules`.
@@ -17,14 +18,14 @@ repositories {
 def DEFAULT_COMPILE_SDK_VERSION = 23
 def DEFAULT_BUILD_TOOLS_VERSION = "23.0.2"
 def DEFAULT_TARGET_SDK_VERSION = 23
-def DEFAULT_SUPPORT_LIB_VERSION = "23.1.1"
+def DEFAULT_SUPPORT_LIB_VERSION = "28.0.0"
 
 android {
-    compileSdkVersion rootProject.hasProperty('compileSdkVersion') ? rootProject.compileSdkVersion : DEFAULT_COMPILE_SDK_VERSION
+    compileSdk rootProject.hasProperty('compileSdkVersion') ? rootProject.compileSdkVersion : (rootProject.hasProperty('compileSdk') ? rootProject.compileSdk : DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion rootProject.hasProperty('buildToolsVersion') ? rootProject.buildToolsVersion : DEFAULT_BUILD_TOOLS_VERSION
 
     defaultConfig {
-        minSdkVersion 15
+        minSdkVersion rootProject.hasProperty('minSdkVersion') ? rootProject.minSdkVersion : 15
         targetSdkVersion rootProject.hasProperty('targetSdkVersion') ? rootProject.targetSdkVersion : DEFAULT_TARGET_SDK_VERSION
         versionCode 1
         versionName "1.0"
@@ -40,7 +41,8 @@ android {
 buildscript {
     if (project == rootProject) {
         repositories {
-            jcenter()
+            google()
+            mavenCentral()
         }
         dependencies {
             classpath 'com.android.tools.build:gradle:1.5.0'
@@ -53,15 +55,16 @@ buildscript {
 
 allprojects {
     repositories {
-        jcenter()
+        google()
+        mavenCentral()
     }
 }
 
-def supportVersion = rootProject.hasProperty('supportLibVersion') ? rootProject.supportLibVersion : DEFAULT_SUPPORT_LIB_VERSION
+def supportVersion = rootProject.hasProperty('supportLibVersion') ? (rootProject.supportLibVersion.toInteger() > 28 ? "28.0.0" : rootProject.supportLibVersion) : DEFAULT_SUPPORT_LIB_VERSION
 
 dependencies {
     implementation fileTree(dir: 'libs', include: ['*.jar'])
     testImplementation 'junit:junit:4.12'
-    implementation "com.android.support:appcompat-v7:${supportVersion}"
+    // Removido appcompat-v7 pois não é necessário para React Native e causa conflitos
     implementation 'com.facebook.react:react-native:+'
 }
